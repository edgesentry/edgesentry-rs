# edgesentry-image-utils

Shared image processing utilities — ONNX inference and OpenCV operations.

## Feature flags
- `onnx` — ONNX Runtime bindings (C dependency)
- `opencv` — OpenCV bindings (C dependency)

Both are optional; crate has no functionality without at least one enabled.
